问题等价于把所有 $-1$ 改成 $-c$ 时序列的最大子段和。

对于一个含有 $x$ 个 $-1$，其它元素总和为 $y$ 的区间，我们把它看成平面上的点 $(x,y)$。要求 $-cx+y$ 的最大值，也就是对 $(x,y)$ 要维护除一个凸包。

考虑分治，维护 $[l,mid]$ 所有后缀的凸包和 $[mid+1,r]$ 所有后缀的凸包，然后 minkowski 合并起来。

最后再把所有 $\mathcal{O}(n\log n)$ 个点合并起来。

时间复杂度 $\mathcal{O}(n\log^2n)$（未精细实现)，类似题 CF1019E（边分治）。
