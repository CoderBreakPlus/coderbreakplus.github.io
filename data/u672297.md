考虑将序列从左往右 dp，记录当前放入了哪些元素以及二分 LIS 算法所维护的序列，状态数 $\matchal{O}(3^n)$，仅能通过 $n \le 18$ 的部分分。

上述算法的瓶颈在于需要记录一个 $2^n$ 状态来保证序列存在 $a$ 作为子序列，其实没有利用 $a$ 序列值域随下标递增的信息。

考虑转置一下，按值从 $1\sim n$ 插入，每次枚举 $v$ 在 $1 \sim v-1$ 之中的相对位置。此时加一维表示上一个关键的值目前的相对位置在哪里即可。

时间复杂度 $\mathcal{O}(T 2^n n^2)$。