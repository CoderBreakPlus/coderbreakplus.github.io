大字符集 AC 自动机板子题。

但是有一些处理细节感觉很能学习：

+ 为了防止当前匹配滚木而不知道当前在哪个点，应该一开始就把所有 $1 \sim n$ 的单个字符插入 AC 自动机。
+ 然后有一些点被 ban 掉。**需要 ban 掉一个点及其 fail 树上的所有后代。**
+ bfs 到一个点的时候再把 fail 的后继信息拷贝过来。如果在父亲出就下传给儿子会出错，因为父亲的信息还没填好。
+ 如果用可持久化线段树，注意 0 号点的 corner case。
+ **不要显式建图**，线段树结点存了左右儿子了，不必再连边了。


