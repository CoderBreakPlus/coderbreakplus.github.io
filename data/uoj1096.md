按照前后缀字典序比较是几乎正确的，但是当串是回文串的时候会错。

因此我们考虑当回文串长度为 $x$ 的之后，截取 $[x/2,x]$ 的前后缀来比较就对了。

倍增分块，截取任意长度为 $2^i$ 的后缀来处理，刚好和倍增求 SA 一起做了。

有了字典序排序的结果之后直接 zkw 线段树就能维护。

去重一些询问后时间复杂度 $\mathcal{O}(n\log^2 n + q\log n)$。