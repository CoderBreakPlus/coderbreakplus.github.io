难点在于发现这个题是暴力算 SG 函数而不是神秘结论。

注意到第一次操作后，区间划分成的子区间的总的可能是只有 $O(n|\Sigma|)$ 种，因为都是某个左（右）端点开始极长的不包含某个字符的串，那么把每个这样的串都算一下就好了。

具体的，计算 $[l,r]$ 的时候，枚举删去的字符 $c$，特殊处理左右两段，中间可以前（后）缀 XOR 和预处理。按照 $l$ 从大到小的顺序算即可。

时间复杂度 $O(n |\Sigma|^2)$，代码很短可以看看。
